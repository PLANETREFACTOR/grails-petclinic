buildCache {
    remote(HttpBuildCache) {
        url = 'http://localhost:8888/repository/remote-build-cache/'
        allowInsecureProtocol = true
        credentials {
            username = 'build-cache'
            password = 'admin'
        }
        push = true
    }
}